<UserControl
    x:Class="Telegram.Controls.UserProfileMusicSheet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Telegram.Controls"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    Visibility="Collapsed">

    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid
            x:Name="Overlay"
            Background="#80000000"
            Tapped="Overlay_Tapped" />

        <Grid
            x:Name="SheetPanel"
            Height="440"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
            Tapped="SheetPanel_Tapped">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="SheetTransform" Y="440" />
            </Grid.RenderTransform>

            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Drag handle strip  -->
            <Grid
                x:Name="DragArea"
                Grid.Row="0"
                Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                PointerCanceled="DragArea_PointerReleased"
                PointerCaptureLost="DragArea_PointerReleased"
                PointerMoved="DragArea_PointerMoved"
                PointerPressed="DragArea_PointerPressed"
                PointerReleased="DragArea_PointerReleased">
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Rectangle
                        Width="60"
                        Height="3"
                        Fill="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                        Opacity="0.6"
                        RadiusX="0"
                        RadiusY="0" />
                </Grid>
            </Grid>

            <!--  Search  -->
            <TextBox
                x:Name="SearchBox"
                Grid.Row="1"
                Margin="12,0,12,6"
                PlaceholderText="Search music..."
                TextChanged="SearchBox_TextChanged" />

            <Grid Grid.Row="2" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
                <ProgressRing
                    x:Name="LoadingRing"
                    Width="34"
                    Height="34"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource SystemControlHighlightAccentBrush}"
                    IsActive="False"
                    Visibility="Collapsed" />

                <TextBlock
                    x:Name="EmptyText"
                    Margin="24"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="14"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    Text="No music in this profile."
                    TextAlignment="Center"
                    Visibility="Collapsed" />

                <ListView
                    x:Name="MusicList"
                    Padding="4,0,4,0"
                    HorizontalContentAlignment="Stretch"
                    Background="Transparent"
                    IsItemClickEnabled="False"
                    SelectionMode="None"
                    Visibility="Collapsed">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="MinHeight" Value="64" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="8,0,8,0" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <local:FfmpegMusicPlayerControl
                                    Grid.Column="0"
                                    MaxWidth="264"
                                    Height="64"
                                    HorizontalAlignment="Stretch"
                                    DurationSeconds="{Binding MediaDurationSeconds}"
                                    IsDownloading="{Binding IsMediaDownloading}"
                                    MediaKind="{Binding MediaKind}"
                                    NextRequested="MusicPlayer_NextRequested"
                                    PlaybackEnded="MusicPlayer_PlaybackEnded"
                                    PlaybackStarted="MusicPlayer_PlaybackStarted"
                                    PreviousRequested="MusicPlayer_PreviousRequested"
                                    SourceRequested="MusicPlayer_SourceRequested"
                                    SourceUri="{Binding MediaFileUri}" />

                                <Button
                                    x:Name="MoreButton"
                                    Grid.Column="1"
                                    Width="40"
                                    Height="40"
                                    Padding="0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0">
                                    <Button.Flyout>
                                        <Flyout>
                                            <StackPanel>
                                                <Button
                                                    Click="ForwardTrackButton_Click"
                                                    Content="Forward"
                                                    HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Left"
                                                    MinHeight="40"
                                                    Padding="12,10"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    FontSize="14" />
                                                <Button
                                                    Click="DownloadTrackButton_Click"
                                                    Content="Download"
                                                    HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Left"
                                                    MinHeight="40"
                                                    Padding="12,10"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    FontSize="14" />
                                            </StackPanel>
                                        </Flyout>
                                    </Button.Flyout>
                                    <FontIcon
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="16"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumHighBrush}"
                                        Glyph="&#xE712;" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
