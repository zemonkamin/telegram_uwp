<UserControl
    x:Class="Telegram.Controls.ChatPickerSheet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Telegram.Controls"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch">

    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid
            x:Name="Overlay"
            Background="#80000000"
            Tapped="Overlay_Tapped" />

        <Grid
            x:Name="SheetPanel"
            Height="440"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
            Tapped="SheetPanel_Tapped">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="SheetTransform" Y="440" />
            </Grid.RenderTransform>

            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                x:Name="DragArea"
                Grid.Row="0"
                Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                PointerCanceled="DragArea_PointerReleased"
                PointerCaptureLost="DragArea_PointerReleased"
                PointerMoved="DragArea_PointerMoved"
                PointerPressed="DragArea_PointerPressed"
                PointerReleased="DragArea_PointerReleased">
                <Rectangle
                    Width="60"
                    Height="3"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Fill="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    Opacity="0.6"
                    RadiusX="0"
                    RadiusY="0" />
            </Grid>

            <local:ForwardPickControl
                x:Name="Picker"
                Grid.Row="1"
                Margin="12,0,12,8"
                HeaderText="Forward to"
                ShowFolders="False" />
        </Grid>
    </Grid>
</UserControl>
